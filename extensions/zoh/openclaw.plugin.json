{
  "id": "zoh",
  "name": "Zoh",
  "description": "Knowledge graph probing tools for navigating web fragments ranked by engagement.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string",
        "description": "Base URL of the Zoh backend API",
        "default": "http://localhost:5001"
      },
      "timeoutMs": {
        "type": "number",
        "description": "Request timeout in milliseconds",
        "default": 15000,
        "minimum": 1000,
        "maximum": 60000
      }
    }
  },
  "uiHints": {
    "baseUrl": {
      "label": "Zoh Backend URL",
      "help": "Base URL of the Zoh backend API (e.g. http://vg-db-2.railway.internal:8080)",
      "placeholder": "http://localhost:5001"
    },
    "timeoutMs": {
      "label": "Request Timeout (ms)",
      "help": "Timeout for API requests to the Zoh backend",
      "advanced": true
    }
  }
}
